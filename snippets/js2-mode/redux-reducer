# -*- mode: snippet -*-
# name: reducer
# key: reducer
# --
import {loop, Effects} from 'redux-loop';
import {Map} from 'immutable';

export const initialState = Map({
  $0
});

// state changes with side effects
// for use with redux-loop
// must return a promise that resolves to action,
// MUST NOT THROW

// action types

// action creators

export default function ${1}Reducer(
  state = initialState,
  action = {}) {
  
  switch (action.type) {
    default:
      return state;
  }
}
