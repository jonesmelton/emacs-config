# -*- mode: snippet -*-
# name : react_component
# key : comp
# --
import React from 'react';
import {
  View,
} from 'react-native';

const ${1:enumerate} = React.createClass({
  render() {
    return (
      <View>
        $0
      </View>
    );
  }
});

export default $1;